// number, string, boolean, object, undefined, any

let myName: string;
let myAge: number;

myName = "Abdul Wahab";
myAge = 20;

let info:any = `My name is ${myName} and I am ${myAge} years old`;

console.log(info);
